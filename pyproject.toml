[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "portfolio-vanessa"
version = "1.0.0"
description = "Flask portfolio app for Vercel deployment"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "main:app"
